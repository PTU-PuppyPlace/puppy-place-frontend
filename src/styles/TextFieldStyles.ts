import theme from "./theme";

const TextFieldStyle = {
  focusColor: theme.primary.p100,
  err: theme.danger.d100,
  bottomBorderColor: theme.gray.g20,
  TextFieldDone: theme.gray.g100,
  disabledBackgroundColor: theme.background,
};

export const TextFieldStyles = { TextFieldStyle };
